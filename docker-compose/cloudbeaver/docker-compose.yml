version: "3.8"

services:
  cloudbeaver:
    image: dbeaver/cloudbeaver:latest
    container_name: cloudbeaver
    ports:
      - "8978:8978"   # Puerto web
    volumes:
      - ./cloudbeaver-workspace:/opt/cloudbeaver/workspace
    restart: unless-stopped
